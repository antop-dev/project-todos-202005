server:
  servlet:
    encoding:
      force: true
      force-request: true
      force-response: true
      charset: UTF-8

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: debug

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: todo
